---
sudo: required

language: python

before_install:
  - openssl aes-256-cbc -K $encrypted_7161553f13bb_key -iv $encrypted_7161553f13bb_iv -in molecule/default/clouds.yml.enc -out molecule/default/clouds.yml -d

install:
  - pip install -r test-requirements.txt

script:
  - tox

notifications:
  irc:
    channels:
      - "irc.freenode.org#osism"
    use_notice: true
